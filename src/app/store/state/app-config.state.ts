import { IAppConfig } from 'src/app/utils/interfaces/config-state.interafec';

export const initialGlobalConfig: IAppConfig = {
    config: {
        adminName: 'Dkay',
        permissions: ['Denied']
    }
}